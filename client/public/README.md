# Public Assets

Place production assets in this directory. Files are served from `/public/` at runtime.

## Images
- `favicon.png` – site favicon
- `thumbs/` – video thumbnail images

## Videos
- `videos/` – user-generated videos (max 10 seconds each)

For development, update asset paths in:
- `client/public/index.html` – favicon link
